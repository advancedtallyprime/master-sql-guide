{
  "name": "master-sql-guide",
  "version": "1.0.0",
  "description": "The Master SQL & MySQL Learning Guide - Production-grade curriculum, enterprise schema, exercises, and interview prep.",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "deploy": "npm run docs:build && wrangler pages deploy .vitepress/dist --project-name=master-sql-guide --branch=main",
    "preview": "npm run docs:build && wrangler pages dev .vitepress/dist"
  },
  "devDependencies": {
    "mermaid": "^11.17.2",
    "puppeteer-core": "^25.10.0",
    "vitepress": "^1.5.0",
    "vitepress-plugin-mermaid": "^2.0.17",
    "wrangler": "^4.131.1"
  }
}
